# MC Hub Environment Configuration

# (REQUIRED) Anthropic Configuration for agentic workflows
ANTHROPIC_API_KEY=

# (Optional) Claude Code Path - if 'claude' doesn't work, run 'which claude' and paste that value here
CLAUDE_CODE_PATH=claude

# (Optional) GitHub PAT - only needed if ADW should use a different account than 'gh auth login'
GITHUB_PAT=

# (Optional) Keeps Claude Code in the root directory after every command
CLAUDE_BASH_MAINTAIN_PROJECT_WORKING_DIR=true

# (Optional) E2B Cloud Sandbox Environment for isolated code execution
E2B_API_KEY=

# (Optional) Cloudflare R2 for uploading review screenshots
# If not configured, local file paths will be shown in GitHub comments instead
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_R2_ACCESS_KEY_ID=
CLOUDFLARE_R2_SECRET_ACCESS_KEY=
CLOUDFLARE_R2_BUCKET_NAME=
CLOUDFLARE_R2_PUBLIC_DOMAIN=
